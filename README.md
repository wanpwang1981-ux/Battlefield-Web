# 陸軍棋 v2.2 (完整規則版)

這是一個基於網頁的陸軍棋遊戲專案。整個遊戲使用原生的 HTML, CSS, 和 JavaScript 打造，沒有依賴任何外部函式庫或框架，讓您能輕鬆地在瀏覽器中直接運行。

## 如何開始遊戲

1.  下載或複製此專案的所有檔案。
2.  在您的電腦上，直接用任何現代瀏覽器（例如 Chrome, Firefox, Edge）開啟 `index.html` 檔案。
3.  遊戲載入後即可開始遊玩。

## 遊戲特色

-   **兩種遊戲模式**：提供「玩家對戰 (PvP)」和「電腦對戰 (PvAI)」兩種模式。PvP 模式為本機輪流操作（Hot-seat）。
-   **秘密佈陣**：每局遊戲開始時，棋子會依據規則隨機佈署，您無法知道對手棋子的確切位置。
-   **完整遊戲介面**：包含可滾動的遊戲日誌、雙方被吃的棋子列表，以及顯示當前狀態的資訊列。
-   **完整戰鬥規則**：實現了司令、炸彈、地雷、工兵等所有棋子之間的克制關係。
-   **基礎遊戲AI**：電腦對手會優先攻擊可吃掉的棋子，若無則隨機移動。

## 核心遊戲規則

-   **遊戲目標**：吃掉對手的 **軍旗** (Flag)。
-   **移動規則**：除了 **軍旗** 和 **地雷** 無法移動外，所有棋子都只能向相鄰的上、下、左、右格子移動一格。
-   **戰鬥規則**:
    -   **比大小**：階級高的棋子可以吃掉階級低的棋子（司令 > 軍長 > 師長 > 旅長 > 團長 > 營長 > 連長 > 排長 > 工兵）。
    -   **同歸於盡**：階級相同的棋子相遇時，兩者同時從棋盤上移除。
    -   **炸彈 (Bomb)**：與任何棋子相遇時，兩者一同移除。
    -   **地雷 (Landmine)**：任何棋子主動攻擊地雷時，攻擊方會被移除，地雷則留在原位。
    -   **工兵 (Engineer)**：是唯一可以安全移除地雷的棋子。
-   **勝利條件**：
    1.  成功吃掉對方的 **軍旗**。
    2.  輪到對方走棋時，對方沒有任何可以合法移動的棋子。
-   **失敗條件**：
    1.  己方的 **軍旗** 被吃掉。
    2.  輪到自己走棋時，沒有任何可以合法移動的棋子。

## 後端 AI 訓練環境 (進階)

本專案包含一個基礎的 Node.js 後端伺服器，用於未來 AI 的訓練與測試。

### 設定與執行

1.  **安裝 Node.js**: 請先確保您的電腦上已安裝 Node.js (建議版本 16 或以上)。
2.  **安裝依賴套件**:
    - 打開終端機 (Terminal) 或命令提示字元 (Command Prompt)。
    - 切換到 `backend` 資料夾: `cd backend`
    - 執行安裝指令: `npm install`
3.  **啟動伺服器**:
    - 在 `backend` 資料夾中，執行啟動指令: `npm start`
    - 看到 `伺服器正在 http://localhost:3000 上運行` 的訊息即表示成功。
4.  **測試 (可選)**:
    - 您可以使用 `curl` 或 Postman 等工具來測試 API 端點。

### AI 訓練與分析 (遺傳演算法)

本專案包含一個後端 AI 訓練腳本 `backend/train.js`，它使用遺傳演算法來自動優化 AI 的決策模型（評估函式的權重）。

#### 運作原理
1.  **產生族群**: 腳本會建立一個由多個 AI 組成的初始「族群」，每個 AI 都有一套隨機的權重參數。
2.  **舉辦聯賽**: 讓族群中的 AI 進行多場高速的「電腦對電腦」對戰，並計算每個 AI 的勝率。
3.  **演化**: 根據勝率，腳本會淘汰表現差的 AI，並讓表現好的 AI 的權重進行「交配」和「突變」，產生更強的下一代。
4.  **重複**: 這個過程會自動重複執行數個世代，最終「演化」出一個擁有最佳權重參數的冠軍 AI。

#### 如何執行 AI 訓練
1.  **啟動訓練**:
    - 打開一個終端機。
    - 切換到 `backend` 資料夾: `cd backend`
    - 執行訓練腳本: `node train.js`
2.  **觀察過程**:
    - 您會在終端機上看到每一代訓練的進度，以及該代最強 AI 的勝率。
    - 整個過程可能需要幾分鐘，具體時間取決於您的電腦性能。
3.  **獲取冠軍 AI 權重**:
    - 訓練結束後，腳本會將最終代最強的 AI 的權重參數，自動儲存到 `backend/champion_weights.json` 檔案中。
    - 同時，終端機上也會印出這組權重參數。
4.  **手動升級遊戲內 AI**:
    - 您可以將 `champion_weights.json` 檔案的內容提供給我，我會將它更新到 `common/gameLogic.js` 中的 `DEFAULT_AI_WEIGHTS`，從而永久性地提升遊戲內「困難」模式 AI 的智慧。
