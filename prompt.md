# 專案執行日誌

此檔案用於記錄 AI (Jules) 在開發「WebLuzhanqi (網頁版陸軍棋)」專案時的每一步執行過程與結果。

---

## 階段 1 (v1.0): 專案設定

*   **Prompt**: 建立 `prompt.md` 檔案，用於記錄所有執行的步驟與結果。
*   **結果描述**: 成功建立 `prompt.md` 檔案，並寫入初始標題和說明。

---

## 階段 1.1 (v1.0): 規則確認

*   **Prompt**: 我會上網搜尋陸軍棋「兩人暗棋」的規則，並將棋子階級、特殊棋子功能、棋盤結構、移動與吃子規則、勝負條件等，以Markdown格式總結後向您報告。
*   **結果描述**: 成功透過 Google 搜尋和閱覽維基百科，整理出完整的兩人暗棋規則，並以 Markdown 格式呈現給使用者，已獲得確認。

---

## 階段 1.2 (v1.0): 可行性分析

*   **Prompt**: 分析僅使用HTML/JS/CSS實現本機雙人對戰的可行性，並說明未來若要擴展為線上對戰所需的技術。
*   **結果描述**: 分析完成。結論是純前端方案完全可行於本機對戰，但線上對戰需要Node.js和WebSocket等後端技術。分析結果已向使用者報告。

---

## 階段 1.3 (v1.0): 專案結構

*   **Prompt**: 建立專案的基礎檔案結構，包含 `index.html`, `README.md`, `css/style.css`, `js/game.js`, 以及 `assets/images/` 資料夾。
*   **結果描述**: 成功建立所有必要的檔案 (`index.html`, `README.md`, `css/style.css`, `js/game.js`) 和資料夾 (`css`, `js`, `assets/images`)。已透過 `ls -R` 指令驗證結構正確無誤。

---

## 階段 1.4 (v1.0): UI/UX 規劃

*   **Prompt**: 在 `README.md` 中規劃遊戲介面，包含棋盤、玩家資訊，並新增遊戲模式選擇（玩家對戰 vs. 電腦對戰）的規劃。
*   **結果描述**: 成功將 UI/UX 的規劃內容（包含遊戲模式選擇、主畫面、狀態區、玩家資訊區等）寫入 `README.md`。期間發現 `README.md` 內容有誤，已使用覆蓋寫入的方式修正，並已驗證內容正確。

---

## 階段 1.5 (v1.0): 核心流程圖

*   **Prompt**: 使用 Mermaid 語法繪製核心流程圖，需能反映遊戲模式選擇的流程，並將其新增至 `README.md`。
*   **結果描述**: 成功使用 Mermaid 語法建立了一個反映遊戲核心邏輯（包含模式選擇、遊戲循環、玩家與AI行動判斷）的流程圖，並已將其新增至 `README.md` 檔案中。已驗證檔案內容無誤。

---

## 階段 1.6 (v1.0): AI 對手設計

*   **Prompt**: 設計一個基礎的電腦 AI。初步策略為：優先攻擊可吃的棋子；若無，則隨機進行合法移動。
*   **結果描述**: 成功設計了基礎AI的行動邏輯，包含生成移動、分類移動（攻擊/普通）和決策制定（優先攻擊，否則隨機移動），並將詳細設計寫入 `README.md`。已驗證檔案內容無誤。

---

## 階段 2.1 (v1.0): 基礎 HTML 結構

*   **Prompt**: 建立 `index.html` 的基本骨架，包含遊戲模式選擇介面、棋盤容器等。
*   **結果描述**: 成功更新 `index.html`，加入了完整的遊戲介面骨架。這包括一個初始的遊戲模式選擇畫面 (`#game-mode-selection`) 和一個預設隱藏的主遊戲畫面 (`#main-game-screen`)，後者又包含了標題、狀態列、主內容區（棋盤和玩家資訊區）。結構已驗證無誤。

---

## 階段 2.2 (v1.0): 基礎 CSS 樣式

*   **Prompt**: 編寫棋盤、棋子和介面的基礎樣式。
*   **結果描述**: 成功將基礎的 CSS 樣式寫入 `css/style.css`。這些樣式涵蓋了主容器、遊戲模式選擇畫面、主遊戲畫面的 Flexbox 佈局（包含標題、狀態列、玩家資訊區）以及棋盤容器的基本外觀。已驗證檔案內容無誤。

---

## 階段 2.3 (v1.0): 遊戲初始化邏輯

*   **Prompt**: 實作遊戲初始化函式，定義資料結構、隨機佈局棋子，並渲染初始棋盤。
*   **結果描述**: 在 `js/game.js` 中成功實作了完整的遊戲初始化流程。這包括：
    1.  定義了遊戲所需的所有資料結構（棋子、棋盤、遊戲狀態）。
    2.  建立了 `createInitialPieces` 函式來生成雙方的棋子。
    3.  實作了 `placePiecesForPlayer` 函式，能夠根據規則（軍旗、地雷、炸彈的特殊位置）隨機佈局棋子。
    4.  擴充了 `renderBoard` 函式，使其能夠將棋盤狀態渲染到畫面上。
    5.  將以上邏輯整合進 `startGame` 函式中。
    程式碼已驗證。

---

## 階段 2.4 (v1.0): 玩家移動邏輯

*   **Prompt**: 實作人類玩家點擊棋子、移動、吃子的核心遊戲邏輯。
*   **結果描述**: 在 `js/game.js` 中成功實作了完整的玩家移動功能。這包括：
    1.  建立了棋盤點擊事件監聽器 `handleBoardClick`。
    2.  實現了棋子的選取和取消選取邏輯，並透過 CSS class 進行高亮顯示。
    3.  建立了簡化版的 `isValidMove` 函式，用於判斷移動合法性（目前僅限相鄰移動）。
    4.  建立了 `determineBattle` 函式，用於處理所有吃子和對戰的規則。
    5.  建立了 `handleMove` 函式，用於更新遊戲狀態（移動棋子、處理戰鬥結果、切換玩家）。
    6.  將所有邏輯整合，讓玩家可以進行基本的遊戲流程。
    程式碼已驗證。

---

## 階段 2.5 (v1.0): 電腦 AI 邏輯

*   **Prompt**: 實作基礎的電腦 AI，讓電腦可以根據設計好的策略自動下棋。
*   **結果描述**: 在 `js/game.js` 中成功實作了電腦 AI 的核心邏輯。這包括：
    1.  建立了 `triggerAIMove` 函式，用於執行 AI 的回合。
    2.  在 AI 函式中，實作了搜尋所有合法移動、將移動分類為「攻擊」和「普通」移動、並根據「攻擊優先」的策略選擇移動的演算法。
    3.  修改了遊戲主循環，在輪到 AI 時能自動觸發 `triggerAIMove` 函式。
    4.  加入了短暫的延遲，讓 AI 的行動看起來更自然。
    程式碼已驗證，基礎的電腦對戰功能已完成。
